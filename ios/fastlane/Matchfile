# Match configuration for code signing
# See: https://docs.fastlane.tools/actions/match/

git_url(ENV["MATCH_GIT_URL"] || "https://github.com/your-org/certificates.git")

storage_mode("git")

type("appstore") # The default type, can be: appstore, adhoc, enterprise, development

# app_identifier(["com.yourcompany.myapp", "com.yourcompany.myapp.widgets"])
# username("your@email.com")
